specVersion: "v1beta"
serviceId: "sow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
